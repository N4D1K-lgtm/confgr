
        [service]
        url = "http://example.com/api"
        enabled = true
        ports = [8080, 8081]
        [service.metadata]
        environment = "production"
        version = "1.0.0"

        features = ["alpha", "beta"]
        [parameters]
        retries = "3"
        timeout = "100"
        
